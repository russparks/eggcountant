<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /egg/
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]
  RewriteRule ^ /egg/index.html [L]
</IfModule>
